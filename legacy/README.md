# 舊版網站備份 (legacy)

這是 2026-08-19 重新開發前的原始網站快照，純 HTML/CSS。

- `index.html` / `index.css`：原始頁面
- `icons/`、`img/`：原始素材
- `CNAME.txt`：原始自訂網域設定

其他還原方式：
- git 分支：`legacy-site`
- git 標籤：`legacy-v1`（對應 commit 728095e）
